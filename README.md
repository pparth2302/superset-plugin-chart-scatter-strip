# superset-plugin-chart-scatter-strip
A custom visualization plugin for Apache Superset that renders scatter plots as a horizontal strip of small multiples (1×N layout) using Apache ECharts.  Designed for industrial analytics, this chart is especially useful for comparing multiple parallel processes (e.g., machine nests, lanes, or stations) across a shared axis.
